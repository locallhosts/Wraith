# Wraith tests

Run the full Python suite with `python -m pytest tests -q`.

The advanced feature tests cover mutation generation/scoring, normalized rule
diffing, ATT&CK Navigator export, schema drift, correlation manifests,
quality scoring, and organization-owned compliance mappings.
